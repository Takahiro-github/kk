.tweets-contents-box
  - @tweets.each do |tweet|
    .tweets-contents-box__contents{style: "background-image: url(#{tweet.image});"}
      %p= tweet.text
      %span.name
        = tweet.name
  = link_to new_tweet_path, class: "btn-circle-fishy" do
    Tweet
